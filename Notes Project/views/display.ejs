<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= filename %></title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body class = "bg-gray-900 text-gray-200">
    <a class = "mt-2 text-blue-500 inline-block"href="/">Go back to home page</a>
    <h1 class = "mt-4 ml-3 text-4xl font-bold"><%= filename %></h1>
    <p class = "mt-6  ml-3 text-lg text-light">
        <% if (fileData) { %>
            <%= fileData %>
        <% } else { %>
            No data to display.
         <% } %>
    </p>
</body>
</html>